import 'package:flutter/material.dart';

class DetalhesPublicacao extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Detalhes"),
        centerTitle: true,
      ),
      body: Container(
        color: Colors.green,
      ),
    );
  }
}
